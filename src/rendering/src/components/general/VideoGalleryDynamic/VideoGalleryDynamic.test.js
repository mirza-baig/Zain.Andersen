// Global
import { hasDataComponent, snapshot } from 'lib/jest/test-utils';
import 'lib/jest/match-media-mock';
// Local
import VideoGalleryDynamic from './VideoGalleryDynamic';
import defaultData from './VideoGalleryDynamic.mock-data';

it('renders correctly', async () => {
  // const component = snapshot(VideoGalleryDynamic, { componentProps: defaultData });
  // await hasDataComponent(component, 'general/videogallerydynamic');
});
