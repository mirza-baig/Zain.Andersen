// Global
import { hasDataComponent, snapshot } from 'lib/jest/test-utils';
// Local
import PageMashupDynamic from './PageMashupDynamic';
import defaultData from './PageMashupDynamic.mock-data';

it('renders correctly', async () => {
  // const component = snapshot(PageMashupDynamic, { componentProps: defaultData });
  // await hasDataComponent(component, 'general/pagemashupdynamic');
});
